# Parser Coverage And Resource Model Matrix

This document is the working checklist for Milestone 5. The high-level roadmap
matrix answers "which feature family owns this"; this matrix answers "what
parser or resource-model slice should we validate next".

WC remains the behavior reference. WCX should keep the implementation layered:
WzLib owns binary format knowledge, Core owns resource identity / inspection /
diagnostics contracts, and App consumes Core services.

## Priority Scale

- P0: blocks Compare/Search/export correctness or common real-client browsing.
- P1: important for broad real-client coverage, but can follow the P0 slices.
- P2: useful compatibility or polish, not required before M6.

## Package Formats

| Area | WC reference behavior | WCX current state | Gap | Priority | Validation path |
| --- | --- | --- | --- | --- | --- |
| PKG1 header | Detect PKG1, encrypted version, header size, version/hash profile | Implemented and tested | Keep regression coverage for boundary offsets and missing encrypted version | P0 | Synthetic WzLib tests plus local `Base/Base.wz` smoke |
| PKG1 directory entries | Recursive directories, string-reference names, checksums, offsets | Implemented for current GMS smoke path; negative directory entry counts, out-of-file string-reference names, expanded entry-count directory tables, and encrypted-version packages with unverifiable hash offsets now have deterministic tests | Add more edge-case fixtures for duplicate names and real large-table smoke when samples expose them | P0 | Synthetic fixtures, `inspect --debug`, local Map/UI/String packages |
| PKG1 package groups | `Name.ini` / `LastWzIndex` plus numbered shard merge | Implemented in Core package group layer; `.ini`-declared missing or invalid shards emit `wcx.package.group.shardMissing` / `wcx.package.group.shardInvalid` warnings; optional Core GMS smoke validates `Map1.wz` merging `Map1_000.wz` image identity | Add real-client smoke notes for any package group warnings found locally | P0 | Core temp-fixture tests plus optional local `Map1.wz` smoke |
| Base package links | Empty stubs link into sibling package folders | Implemented conservatively for Base-style workspace and same-package relative links; unresolved diagnostics are emitted only when a candidate package path is actually considered and fails, while out-of-scope workspace siblings are ignored without warning | Preserve true source identity as additional linked package shapes appear | P0 | Core tests plus local `Base.wz`, `UI.wz`, `Map.wz` smoke |
| PKG2 header/directory | Detect PKG2, read header hash fields, select version/hash profile, decode directory strings, calculate image offsets | First KMST1199/1200 slice implemented in WzLib/Core. Modern KMS 0x44-byte PKG2 header envelopes inspect through `pkg2_modern_kms` with `pkg2HeaderVariant: modern` and no synthetic `wzVersion`. Synthetic `pkg2_kmst1200` and modern KMS fixtures cover directory entries, CLI output, and image payload inspection. User-supplied KMS smokes validate `pkg2_kmst1200` `Item_000.wz`, modern KMS `Item_000.wz` / `String_000.wz`, and IMG extraction through the shared reader. Unsupported PKG2 profiles still return `wcx.package.pkg2.directoryUnsupported`; local GMS header scan found no PKG2 `.wz` sample among 780 WZ files | Add KMST1196-1198 legacy profile support only with sample/reference-backed tests; after a complete KMS client is available, add representative KMS package-family coverage to the unified `WCX_CLIENT_DATA_DIR` external smoke harness | P0 | WC reference review, synthetic `pkg2_kmst1200` and modern KMS fixtures, user-supplied KMS smoke notes |
| List.wz / optional containers | WC supports older/newer container helpers; `.ms` and `.mn` both flow through `Wz_Structure.LoadMsFile`, which tries the Snow reader before the ChaCha20 reader; MS image entries decrypt through `Ms_Image.OpenRead()` / `Ms_ImageV2.OpenRead()` and then use normal IMG extraction. `Wz_Crypto.LoadListWz` reads `List.wz` as an encrypted string-list helper, removes the `dummy` sentinel, and uses the detected key type for later PKG1 string handling | `.ms` and `.mn` v2/Snow and v4/ChaCha20 container directory tables inspect through Core; folder inspection now lists `.ms`/`.mn` packages alongside `.wz` and preserves the actual container kind in `format`, display value, and `containerKind` metadata; synthetic `.ms` v2/v4 and `.mn` fixtures lock directory and image payload extraction into the shared IMG inspection path; Canvas preview can load direct MS/MN Canvas payloads and `_outlink` targets through the same logical resolver used for WZ packages. `List.wz` first-slice inspection now decodes WC-style entries for no-op/KMS/GMS key profiles, excludes the `dummy` sentinel, auto-detects the string key, and exposes the list through `inspect` as `format: listwz`. Folder inspection skips `List.wz` so the helper is not misreported as an invalid resource package. Local GMS `Data/Packs/*.ms` smoke covers 10 Mob/Skill pack files, verifies `Packs/Skill_00002.ms` -> `Skill/15500.img` extraction, and verifies `Packs/Mob_00000.ms` `_outlink` preview resolution to the local logical Canvas target. Unsupported image payload/parser cases report `wcx.package.ms.imageUnsupported`; unsupported container shapes report `wcx.package.ms.directoryUnsupported`. Local GMS scan found no `List.wz` and no `.mn` files | Support remains required for older-client compatibility. `List.wz` is observable but not yet wired into PKG1 string key/profile selection; real `List.wz` smoke still needs an older-client sample. `.mn` needs real smoke when a sample is available | P1 | Synthetic `List.wz` no-op/KMS/GMS fixtures, synthetic `.ms` v2/v4 fixtures, synthetic `.mn` fixture, optional local GMS `.ms` smoke, WC reference review, future older-client smoke samples |

## IMG Values

| Area | WC reference behavior | WCX current state | Gap | Priority | Validation path |
| --- | --- | --- | --- | --- | --- |
| IMG root object | Lazy extract selected IMG, then full object tree | Implemented for supported object families | Keep App lazy/full flow aligned with Core inspection defaults | P0 | App tests and local GMS UI smoke |
| Property traversal | Nested property tree with scalar values | Implemented with caller depth; UI uses full inspection; deterministic fixtures now cover null, int16 aliases, compressed/expanded int32, compressed/expanded int64, compressed/expanded single, double, string, and empty nested Property nodes through WzLib plus `inspect --debug` | Add more deterministic fixtures only when new WC scalar encodings or real samples expose gaps | P0 | WzLib/Core fixtures, CLI golden output |
| Vector / Convex2D | Decode geometry values and nested components | Metadata/value inspection implemented; optional local GMS smoke validates Character WZ Vector anchors, MS Mob Vector anchors through the shared IMG inspection path, and UI `Shape2D#Convex2D` metadata in `UI_000.wz/RunnerGame.img` | Add more deterministic fixtures only if new geometry encodings appear | P1 | WzLib fixtures, optional local Character/Packs/UI smoke |
| UOL / link strings | Resolve links where possible; display unresolved links | Link-like values now carry `Identity.LinkedTarget`; `inspect --debug` can populate `Identity.ResolvedLinkedTarget` for local `_inlink`, relative UOL, and logical `source` / `_outlink` / `link` targets with `.img` segments; optional Core GMS smoke validates `Map1_000.wz/100000000.img` `miniMap/_outlink`, `UI_000.wz/Basic.img` `Cursor/0/0/_outlink`, and `Packs/Mob_00000.ms` `move/0/_outlink`; optional Avalonia Headless smoke covers the UI `_outlink` App workflow through Resources -> IMG Content -> Preview; Canvas preview uses the same resolver for link-string and UOL selections, including WC-style UOL -> placeholder Canvas -> `_outlink` chains; unresolved Canvas preview links emit `wcx.viewer.canvas.linkUnresolved` | Expand real-client link smoke and add diagnostics for non-viewer unresolved targets only when consumers need them | P0 | Core link fixtures, App preview tests, optional local Map/UI/Packs smoke |
| Canvas metadata / bitmap decode | Width, height, format, scale, payload metadata, BGRA8888 bitmap conversion | Implemented; WzLib Canvas bitmap decode is shared by Preview and CLI Canvas export for direct-zlib ARGB4444, ARGB1555, RGB565 including WC's `scale=4` expansion path, R16, ARGB8888, A8, RGBA1010102, DXT3, DXT5, DXT1, BC7, and RGBA32Float slices. Core Canvas selection/link resolution is also shared between Preview and CLI export, including `.ms` / `.mn` payload extraction, package-group selectors, and supported Canvas links | Add unsupported-format diagnostics where payload metadata is insufficient; keep PNG export and non-direct-zlib payload support for later media/rendering slices | P0 | Synthetic Canvas fixtures, optional local GMS Skill BC7 smoke, local UI/Map smoke, and CLI export tests |
| Sound / Video / RawData | Metadata plus payload extraction/playback/export in WC | RawData and Sound_DX8 metadata only; Canvas#Video parses WC-style `MCV0` header/table metadata including fourCC, size, frame count, flags, and frame offsets. `WzComparerX.Rendering` has selected-frame and full sequence decoders that keep separate color/alpha VP9 states. Core can select Video values through the same image target path used by Canvas. CLI exposes `export --type video --out <directory>` and writes per-frame BGRA dumps plus `manifest.json` when the decoder succeeds. Avalonia Preview can follow selected IMG Content video nodes through the sequence decoder. Optional local GMS smoke validates Sound_DX8 metadata in `Sound_000.wz/AchievementEff.img`, RawData metadata in `UI_000.wz/Login.img`, Canvas#Video metadata in `UI_000.wz/UIGachapon.img`, and `Packs/Mob_00002.ms` `BossFirstAdversary.img`. Current real VP90 sequence smokes reach the decoder but fail on VPDecoder feature gaps: `UIGachapon.img` frame 0 reports coefficient block geometry mismatch, while `BossFirstAdversary.img` frame 1 reports unsupported non-display/reference state. VP8 is recognized as `VP80` metadata but VPDecoder still lacks VP8 pixel reconstruction | Complete VPDecoder VP9 inter-frame coverage and VP8 pixel reconstruction, or add a native libvpx fallback behind the same Rendering interface; keep App/CLI surfaces stable | P1 | Local Sound/UI/Packs smoke, VPDecoder submodule tests, Rendering sequence tests, CLI video export tests, and App video preview tests |
| Lua IMG | Lua-specific stream decode and script export | Synthetic fixture support; parser-based local GMS scan across common package families found no `.lua` image entries | Find real sample in an older or different client, or keep documented as fixture-only | P1 | Fixture tests, future local smoke |
| Text-format IMG | WC text IMG v1/v2 parse/export | Synthetic fixture support; no direct local real-sample entry yet. Local text-format discovery requires image payload inspection rather than directory-name filtering, so broad scans should stay bounded | Find real sample; add multiline v2 behavior if needed | P1 | Fixture tests, future local smoke |

## Resource Identity Contract

| Area | WC reference behavior | WCX current state | Gap | Priority | Validation path |
| --- | --- | --- | --- | --- | --- |
| Node path | Tree nodes are navigable by stable WZ-style paths | `ResourceInspectionNode.Path` is present; identity semantics documented in `docs/resource-identity.md` | Add more cross-package examples as compare/search requirements appear | P0 | Core model tests and CLI JSON golden output |
| Source package | Split/merged image nodes know original package | Core nodes now carry `Identity.PackagePath`; merged shard image identities point to the source shard | Continue applying identity to later linked targets and diagnostics | P0 | Core package-group tests |
| Image selector | Selected IMG can be reloaded without UI path guessing | Core image nodes now carry `Identity.ImageSelector`; CLI debug JSON covers image/value/link identity | Test selector behavior across more linked package shapes | P0 | Core/App tests |
| Value path | Canvas export and preview use explicit property paths | IMG property nodes now carry `Identity.ValuePath`, and debug link resolution can report linked value paths | Add more cross-package examples as compare/search requirements appear | P0 | Export/App tests |
| Linked target | `source`, `_inlink`, `_outlink`, UOL targets should be represented | Core link-like value nodes carry normalized `Identity.LinkedTarget`; `inspect --debug` can include `Identity.ResolvedLinkedTarget` and resolved-link debug metadata for deterministic targets | Add richer unresolved-target diagnostics and real-client smoke for link-heavy packages | P0 | Core fixtures plus App preview tests |
| Diagnostics | Stable severity/source/code/path | Implemented for many parser/export cases; failed split-package candidates emit `wcx.package.link.unresolved`, package-group shard issues emit `wcx.package.group.shardMissing` / `wcx.package.group.shardInvalid`, missing IMG selectors emit `wcx.inspection.image.notFound`, unsupported PKG2 directory profiles emit `wcx.package.pkg2.directoryUnsupported`, `.ms` / `.mn` image payload or downstream IMG inspection failures emit `wcx.package.ms.imageUnsupported`, RawData/Video/Sound metadata emit unsupported payload diagnostics, unresolved Canvas preview links emit `wcx.viewer.canvas.linkUnresolved`, and failed Video sequence decode reports Rendering diagnostics such as `wcx.video.frame.decodeFailed` | Keep future export/playback diagnostics aligned with the same codes/sources | P0 | Unit tests and CLI golden output |

## Real-Client Smoke Matrix

| Package family | Current coverage | Next M5 validation |
| --- | --- | --- |
| Base | Header, top-level directory, Base-style package links | Smoke unresolved-stub diagnostics when a failed local candidate is available |
| Map | M4 UI smoke covers `Map1.wz` group merge and linked Canvas preview; unified optional external-client Core smoke validates `Map1.wz` merged shard image identity and `Map1_000.wz` `_outlink` resolved target identity when that layout is present | Add richer Map/Core smoke as new link or Canvas edge cases appear |
| UI | Directory and Canvas metadata smoke exists; optional Core GMS smoke validates `UI_000.wz/Basic.img` `Cursor/0/0/_outlink` identity resolution through `UI/_Canvas/_Canvas_000.wz` and loads the linked 24x28 format-1 Canvas preview; `UI_000.wz/Login.img` validates RawData metadata; `UI_000.wz/RunnerGame.img` validates real Convex2D metadata; `UI_000.wz/UIGachapon.img` validates real Canvas#Video metadata; optional Avalonia Headless smoke covers the same Resources -> IMG Content -> Preview workflow | Add more UI link/value smoke only when a new unsupported shape appears |
| Item / Character / Skill | Unified optional external-client smoke validates representative package roots can be inspected without parser errors when those files are present; Character smoke validates `Character_000.wz/00002000.img` `_outlink` identity resolution and Canvas preview through `Character/_Canvas/_Canvas_000.wz`, plus `walk1/0/body/origin` and `walk1/0/arm/origin` Vector anchors; Item smoke validates `Item_000.wz/SkillOption.img` property/scalar shape for future item/skill projections | Add representative `inspect --debug` notes for remaining Item/Skill IMG value families when a new downstream projection needs them |
| String | Unified optional external-client smoke validates representative package root can be inspected without parser errors when present; `String_000.wz/Eqp.img` smoke validates the StringLinker-style `Eqp/Cap`, `Eqp/Weapon`, and `Eqp/Accessory` structure | Use as early StringLinker/Search input after M5 identity stabilizes |
| Mob / Npc | Unified optional external-client smoke validates representative package roots can be inspected without parser errors when present; local GMS `Packs/Mob_00000.ms` smoke validates `_outlink` Canvas preview resolution from an MS image into the logical Canvas target, Vector anchors in `Mob/1150000.img`, and Avalonia Headless covers the MS App workflow through Resources -> IMG Content -> Preview | Add more representative IMG/property smoke notes |
| Sound | Sound_DX8 metadata smoke exists; unified optional external-client smoke validates package root can be inspected without parser errors when present and `Sound_000.wz/AchievementEff.img` exposes `GradeUp` Sound_DX8 metadata plus the stable audio unsupported diagnostic | Preserve payload diagnostics before M8 export |
| Effect | Base link path coverage; unified optional external-client smoke validates package root can be inspected without parser errors when present; `Effect_000.wz/BasicEff.img` smoke validates `_outlink` resolution and Canvas preview through `Effect/_Canvas/_Canvas_002.wz` | Add more Effect link smoke only when a new unsupported shape appears |
| Lua / text IMG | Synthetic only; parser-based local GMS scan found no `.lua` image entries in common package families | Find a direct local sample or keep fixture-only status explicit |

## Immediate M5 Slices

1. Decide how the new `List.wz` inspection model should feed PKG1 string
   key/profile detection once a real older-client sample is available.
   `List.wz` remains a string-list helper, not a resource package tree.
2. Expand `.ms` / `.mn` coverage only when a real sample or downstream feature
   exposes a new unsupported IMG value/payload shape; the baseline v2/v4
   directory and image payload extraction slice is now in place.
3. Complete the VPDecoder or native-backend coverage needed for real GMS VP90
   sequences and future VP80 samples now that Core/App/CLI video surfaces are
   wired.
4. Keep future media export/playback diagnostics aligned with the existing
   parser payload diagnostic codes.
5. Extend PKG2 only when a new representative KMST/KMS sample or WC reference
   profile is available; the current accepted slice covers KMST1199/1200
   directory tables, modern KMS 0x44-byte headers, and IMG offsets.
6. Add representative real-client smoke notes for deeper IMG value families in
   Map, UI, Item/Character, String, Mob/Npc, Skill, Sound, and Effect without
   committing client files.
